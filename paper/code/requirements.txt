# Runtime dependencies of the classical pipeline.
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
pandas>=2.0
PyYAML>=6.0

# Testing
pytest>=7.4

# QUBO construction and sampling.  Used by experiments/12_qubo_solver.py and
# tests/test_qubo_solver.py.  dwave-samplers provides the simulated-annealing
# sampler behind the same dimod interface that drives D-Wave hardware, so the
# same QUBO object would be submitted to a quantum annealer unchanged.
dimod>=0.12
dwave-samplers>=1.3

# Not required.  Listed so the intended hardware path is on record; running on
# a real annealer additionally needs dwave-system and an API token.
#   dwave-system>=1.25
